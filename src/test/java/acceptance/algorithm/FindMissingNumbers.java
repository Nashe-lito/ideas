/*
package acceptance.algorithm;

import java.util.Arrays;
import java.util.BitSet;

public class FindMissingNumbers {
  public static void main(String[] args){
   printMissingNumber(new int[]{1, 2, 3, 4, 6}, 6);

   printMissingNumber(new int[]{1, 2, 3, 4, 6, 7, 9, 8, 10}, 10);

   printMissingNumber(new int[]{1, 2, 3, 4, 6, 7, 9, 8}, 10);

   int[] iArray = new int[] {1, 2, 3, 5};
   int missing = getMissingNumver(iArray, 5);
   System.out.printf("Missing number in array %s is %d %n", Arrays.toString(iArray), missing);
  }

  public staticviid printMissingNumber(int[] numbers, int count) {
    int missingCount = count - numbers.length;
    BitSet bitSet = new BitSet(count);
    for (int number : numbers){
      bitSet.set(number - 1);
      System.out.printf("Missing number in array %s",
      Arrays.toString(numbers), count);
      int lastMissingIndex;
    }
  }

}
*/
